# Rust build artifacts
llm-gateway/target/
target/

# Local config with API keys — never commit
llm-gateway/gateway.json
gateway.json
*.local.json

# Freebuff agent state / handoff notes
.freebuff/
PASS.md

# Test artifacts
llm-gateway/*.log
# test config may hold real keys during local testing — keep out of git
llm-gateway/test/gateway.json
llm-gateway/test/stats.json
# runtime stats file (may be created next to any config)
stats.json

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
